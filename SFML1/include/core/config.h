#pragma once

// ============================================================================
// config.h Ч централизованна€ конфигураци€ проекта
// ============================================================================
// «десь задаютс€ глобальные константы и флаги, вли€ющие на поведение игры.
// Ётот файл не содержит логики Ч только настройки и опции.
//
// ¬ будущем можно будет заменить на систему конфигов из JSON или INI.
// ============================================================================

namespace config {

    // -----------------------
    // ќсновные параметры окна приложени€
    // -----------------------
    
    inline constexpr unsigned int WINDOW_WIDTH = 640;
    inline constexpr unsigned int WINDOW_HEIGHT = 480;
    inline constexpr const char* WINDOW_TITLE = "SFML Application";

    // -----------------------
    // »гровые параметры
    // -----------------------
    
    inline constexpr float PLAYER_SPEED = 100.f; // скорость игрока в пиксел€х в секунду

    // -----------------------
    // ќтладка
    // -----------------------
 
    // ѕоказывать ли сообщение при завершении программы (дл€ отладки)
    inline constexpr bool DEBUG_HOLD_ON_EXIT = true;

} // namespace config

